diff --git a/boot/ipl3.c b/boot/ipl3.c
index 41e4d9773..0bae79b4e 100644
--- a/boot/ipl3.c
+++ b/boot/ipl3.c
@@ -196,6 +196,7 @@ static void rsp_bzero_init(bool bbplayer)
 // since the DMA would just wrap around in IMEM.
 void rsp_bzero_async(uint32_t rdram, int size)
 {
+    /*
     // Use RSP DMA which is fast, but only allows for 8-byte alignment.
     // Data outside of the 8-byte alignment is cleared using the CPU,
     // using uncached writes so that they behave exactly the same as RSP DMA would.
@@ -219,6 +220,7 @@ void rsp_bzero_async(uint32_t rdram, int size)
         size -= sz;
         rdram += sz;
     }
+    */
 }
 
 // Callback for rdram_init. We use this to clear the memory banks
@@ -268,7 +270,7 @@ void stage1(void)
     entropy_add(C0_COUNT());
     entropy_add(*DP_CLOCK);    
     C0_WRITE_CAUSE(0);
-    C0_WRITE_COUNT(0);
+    //C0_WRITE_COUNT(0);
     C0_WRITE_COMPARE(0);
 	C0_WRITE_WATCHLO(0);
 
diff --git a/src/entrypoint.S b/src/entrypoint.S
index 04ca89a41..973a27b63 100644
--- a/src/entrypoint.S
+++ b/src/entrypoint.S
@@ -22,6 +22,17 @@ _start:
 	li v0,SR_CU1|SR_PE|SR_FR|SR_KX|SR_SX|SR_UX
 	mtc0 v0,C0_SR
 
+	/* fill .bss with 0s */
+	la a0, __bss_start
+	or a0, 0x20000000           /* convert address to KSEG1 (uncached) */
+	la a1, __actual_bss_end
+	or a1, 0x20000000
+.Lbss_init:
+	sd $0,(a0)
+	addiu a0,8
+	bltu a0,a1, .Lbss_init
+	nop
+
 #if 0
 	/* Run a check to verify that BSS is cleared. This is useful while debugging
 	   IPL3 changes. */
diff --git a/src/joybus/joybus.c b/src/joybus/joybus.c
index 94eaf59a5..b1eb736b7 100644
--- a/src/joybus/joybus.c
+++ b/src/joybus/joybus.c
@@ -97,7 +97,7 @@ static timer_link_t *identify_timer = NULL;
  * the connected devices to check if the identifier has changed or if any
  * accessories have been connected/disconnected.
  */
-#define JOYBUS_IDENTIFY_INTERVAL_TICKS      (1 * TICKS_PER_SECOND)
+#define JOYBUS_IDENTIFY_INTERVAL_TICKS      (TICKS_FROM_MS(20))
 
 /** @brief Detection callback entry */
 typedef struct {
